{
  "name": "in3d-help",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "npm run build && wrangler deploy",
    "deploy:sandbox": "npm run build && node scripts/patch-wrangler-sandbox.mjs && wrangler deploy --config dist/server/wrangler.json --env sandbox"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.6.0",
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.2",
    "wrangler": "^4.95.0"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "sharp": "^0.34.5"
  }
}
